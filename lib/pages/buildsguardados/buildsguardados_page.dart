import 'package:flutter/material.dart';

class BuildsguardadosPage extends StatelessWidget {
  const BuildsguardadosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Estoy en el BuildsguardadosPage',
        style: TextStyle(
          fontSize: 22,
          fontWeight: FontWeight.bold,
          color: Color(0xFF8B0002),
        ),
      ),
    );
  }
}
